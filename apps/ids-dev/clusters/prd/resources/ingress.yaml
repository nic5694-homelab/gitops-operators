apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ids-dev
  namespace: ids-system-dev
spec:
  ingressClassName: nginx
  rules:
  - host: ids.homelab
    http:
      paths:
      - backend:
          service:
            name: ids-dev
            port:
              number: 3000
        path: /
        pathType: Prefix

