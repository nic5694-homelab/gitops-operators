name: adguard
chart: adguard-home
repoURL: https://charts.gabe565.com
version: 0.3.25
namespace: adguard
environments:
  - dev
  - prd
ignoreDifferences:
  # Ignore status field on LoadBalancer Services (managed by K8s/MetalLB, not by us)
  - group: ""
    kind: Service
    jsonPointers:
      - /status
