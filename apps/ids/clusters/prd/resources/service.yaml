apiVersion: v1
kind: Service
metadata:
  name: ids
  namespace: ids-system
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.10
  selector:
    app: ids
  ports:
    - name: http
      port: 3000
      targetPort: 3000
