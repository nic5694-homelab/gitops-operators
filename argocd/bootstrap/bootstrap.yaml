apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nic5694-homelab/gitops-operators.git
    targetRevision: feat/integrate_kuma_waf
    path: argocd
    directory:
      recurse: true
      include: "{project.yaml,applicationset.yaml,resources-applicationset.yaml,clusters/*.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
